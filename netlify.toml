[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  directory = ".netlify/functions"

# Disable Netlify Blobs if not needed
[context.production.environment]
  NEXT_PRIVATE_SKIP_MIDDLEWARE_URL_NORMALIZE = "1"
